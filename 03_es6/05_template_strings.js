/**
 *
 * TEMPLATE STRINGS
 *
 */

const name = 'EcmaScript';
const version = 2015;
const hi = () => 'sier hei!';
const result = `Går over to linjer:
${name} ${version + 1} ${hi()}`;

console.log(result);

// Går over to linjer:
// EcmaScript 2016 sier hei!
